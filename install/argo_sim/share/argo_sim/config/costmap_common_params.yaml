costmap_common_params:
  ros__parameters:
    obstacle_layer:
      enabled: true
      observation_sources: scan
      scan:
        sensor_frame: front_laser_link
        data_type: LaserScan
        topic: scan
        marking: true
        clearing: true
    inflation_layer:
      enabled: true
      inflation_radius: 0.2
